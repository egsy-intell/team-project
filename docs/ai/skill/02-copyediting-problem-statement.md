# Copyediting the problem statement

**Branch:** `main`
**Span:** Jul 22, 09:00 UTC

Placeholder transcript — see `01-fixing-the-split-test.md` for the note
on what belongs here. This thread's manifest entry demonstrates the
other way to attribute commits: listing them explicitly under
`"commits"` instead of letting `ai-disclosure` find them via
`"start"`/`"end"` and `git log`.
